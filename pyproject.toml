[project]
name = "md-hybrid-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "chromadb>=1.5.9",
]
license = { file = "LICENSE" }

[project.optional-dependencies]
tests = [
    "pytest>=9.0.3",
]
